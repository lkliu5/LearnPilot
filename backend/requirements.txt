# B0 骨架最小依赖集（后续阶段按需追加 Chroma / bge / LangGraph 等）
fastapi
uvicorn[standard]
pydantic-settings
python-dotenv
# B1 数据层 + 双角色认证（SQLite 经 SQLAlchemy；JWT 用纯 Python 的 PyJWT，口令哈希用 stdlib pbkdf2，无原生构建依赖）
sqlalchemy
pyjwt
# B2-a P0 主流程（Mock 数据源）：multipart 表单解析 + PDF 文本抽取
python-multipart
pypdf
# B3 RAG 管道：Chroma 持久化向量库 + bge 本地模型 + BM25 稀疏检索
# 说明：sentence-transformers / chromadb 加载失败会自动降级（哈希嵌入 / 内存余弦库 / 仅 RRF），
#       无网络、无模型文件时仍可跑通全链路；rank_bm25 为纯 Python 轻量包。
chromadb
sentence-transformers
rank_bm25
# B5-a LangGraph 多智能体工作流骨架（纯 Mock provider，无密钥可跑）
langgraph
# B5-b DeepSeek 真实生成（OpenAI 兼容协议 SDK）；mock 模式不经过该 SDK、无 Key 可跑
openai
