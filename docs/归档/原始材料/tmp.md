# 智学中枢 V2.0 Dashboard 首页重构指令

## 角色与上下文

你现在是资深前端开发专家。我们将基于「智学中枢 V2.0 视觉升级规范」，对 Dashboard 首页进行高保真布局优化。

**目标**：

- 消除界面松散的空白区域
- 提升信息密度和高级感
- 应用玻璃拟态（Glassmorphism）、统一圆角 (`rounded-2xl`) 和超轻阴影 (`shadow-[0_8px_32px_rgba(15,23,42,0.06)]`)
- 保持科技氛围与动态反馈

---

## 修改 1：重构「推荐学习路径」区域

**当前问题**：

- 右侧有大片空白
- 卡片平铺过于松散

**修改指令**：

1. 找到包裹「推荐学习路径」标题和下方三个卡片的容器 `<div>`。
2. 将单栏布局改为双栏：
   - Flex 或 Grid 布局
   - 宽度比例：`w-2/3` + `w-1/3` 或 `grid-cols-[2fr,1fr]`
3. 右侧新增卡片组件：
   - 标题：`推荐动态`
   - 样式：毛玻璃卡片（`rounded-2xl`，`bg-white/70`，`shadow-[0_8px_32px_rgba(15,23,42,0.06)]`）
   - 内容：
     - 左侧图标（辅助色）
     - AI 建议文本：`基于你的画像诊断，建议加强 '神经网络基础'、'大模型微调' 等核心组件`
     - 最近学习列表：
       ```
       • 注意力机制原理 (45%)
       • 大模型微调前沿 (20%)
       ```

---

## 修改 2：重构「Agent 状态」区域

**当前问题**：

- 大白色容器右侧空白
- 缺少互动内容

**修改指令**：

1. 找到「Agent 状态」大卡片容器 `<div>`。
2. 将布局改为分栏：
   - 左栏：`w-2/3` 保留原有 3 个 Agent 平铺列表
   - 右栏：新增辅助卡片（毛玻璃样式，圆角）
3. 右栏卡片内容：
   - 标题：`互动面板`
   - 内容示例：
     - `画像诊断Agent：我已完成你的诊断，点击复习巩固。[Review Now按钮]`
     - `内容审核校验Agent：校验通过。[查看报告]`

---

## 开发优先级与约束

- 优先完成分栏布局，消除空荡感
- 必须：
  - 有空间感
  - 有视觉重点
  - 保持科技氛围
  - 有动态反馈
- 禁止：
  - 大面积纯白
  - 粗糙阴影
  - 死板表格布局

---

## 输出要求

- 提供修改后的完整 JSX/TSX 代码
- 包括样式和布局更新
- 分栏布局、毛玻璃卡片、辅助卡片、模拟数据都必须包含