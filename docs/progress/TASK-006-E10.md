# TASK-006-E10 康奈尔笔记生成边界拆分

## 完成状态

已完成 TASK-006-E 的第十个渐进拆分边界。将六个核心知识点的康奈尔笔记模板、契约装配、未收录知识点的确定性 Mock 兜底，以及真实模型输出清洗，从 `app/core/llm.py` 抽取到 `app/core/llm_cornell.py`。

TASK-006-E 主任务保持进行中。本轮未修改 `generate_cornell_cues` Provider 编排、DeepSeek Prompt、讲义来源内容、费曼评估、笔记持久化、前端页面或其他能力边界，未进入 TASK-006-F。

本阶段新增文件 3 个，符合每阶段新增文件不超过 8 个的约束。

## 边界与兼容性

- `nn/ml/dl/cnn/transformer/finetune` 六组固定线索、提纲和总结引导逐字保留。
- 模板迁移执行 102 行逐行一致性校验，除公开常量名称外完全一致。
- 线索 `c1...`、提纲 `n1...` 编号和 question 类型的 `cueId` 关联规则保持不变。
- 未收录知识点继续产出 5 条参数化线索、两组提纲和主题总结引导。
- 真实输出继续限制为 5–8 条线索，非法 type 回落 `question`，提纲要点最多 4 条。
- 来源仍从 `llm.py` 的讲义来源列表传入并逐项复制，没有迁移或修改来源职责。
- `_CORNELL_TEMPLATES` 继续在 `app.core.llm` 保留兼容别名；`LLMClient._assemble_cornell`、`_mock_cornell`、`_clean_cornell` 的签名与调用方式保持不变。
- `llm.py` 从 E9 后的 2382 行降至 2221 行。

## 文件清单

新增：

- `backend/app/core/llm_cornell.py`
- `backend/tests/test_llm_cornell.py`
- `docs/progress/TASK-006-E10.md`

修改：

- `backend/app/core/llm.py`
- `docs/维护/工作任务清单.md`
- `docs/维护/当前工程状态.md`

未修改前端、OpenAPI 快照和接口文档。工作区原有欢迎页、登录页、侧边栏、PPT、截图及其他文档改动均保留且不纳入提交。

## 启动命令

```powershell
cd backend
$env:LLM_PROVIDER='mock'
uvicorn app.main:app --port 8000
```

## 验证命令与实测结果

语法检查与专项回归：

```powershell
cd backend
python -m compileall -q app tests
python -m pytest -q tests/test_llm_cornell.py tests/test_c2_learning_flow.py tests/test_content_safety.py tests/test_contract_snapshot.py
```

结果：语法检查退出码 0；专项测试 `97 passed, 1 warning in 9.17s`，0 failed。

全量回归：

```powershell
cd backend
python -m pytest -q
```

结果：`560 passed, 1 skipped, 1 warning in 172.16s`，0 failed、0 errors。唯一 warning 为既有 Starlette/httpx 弃用提示。

## 验收结论

- 本轮只拆分康奈尔确定性模板、Mock 组装与真实输出清洗这一个能力边界。
- Provider 回落策略、Prompt、接口字段、模板文案、编号关联和来源内容保持不变。
- 专项与全量回归均为 0 error。
- TASK-006-E 后续拆分仍须逐边界独立实施、验证和提交。
