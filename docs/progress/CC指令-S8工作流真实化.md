# CC 指令 · S8 — 多智能体工作流真实化（核心资源链路 Mock→真实 · 第 2 项）

> 全局纪律照旧：已联调接口签名禁改、统一信封、Mock/真实双模式、复用优先、完成即停、实测 0 报错。
> 背景：S7 已把 `/resource/lecture` 接到真实 RAG+生成+审核（B5b）管线。本会话让工作流大屏反映**真实的 LangGraph 执行轨迹**，而非 setTimeout 假动画。

═══════════════════════════════════════════════════════════════
# 角色
资深全栈工程师（后端 FastAPI + LangGraph；前端 React + WS/SSE）。项目已联调，禁止改动既有接口签名。本会话把智能体工作流从"模拟动画"切到"真实执行轨迹驱动"。

# 任务 1：先查现状（必做，先报告再决定）
查清并在回复中说明：
1. 工作流大屏（`/workflow` 及其前端页）当前是**写死的**（setTimeout 串行模拟 4 阶段、消息日志硬编码），还是由真实 LangGraph 执行驱动？
2. 接口文档里是否已定义工作流的 **WS/SSE 推送契约**（B7 曾规划）？真实多智能体管线（S7 用的 B5b：诊断/检索/生成/审核各节点）在哪个模块？
若已是真实轨迹驱动，报告确认后跳过任务 2。

# 任务 2：接真实执行轨迹（若现状为 Mock）
让工作流大屏由**真实 LangGraph 运行**点亮：
- **真实节点驱动**：随真实管线（诊断→检索→生成→审核）的实际状态转移，逐节点点亮大屏——节点激活/完成时机来自真实执行，**不再用固定 setTimeout 节奏**。
- **真实消息日志**：每个节点展示**真实 agent 的输出/日志摘要**（如检索命中片段数、生成 token 进度、Critic 幻觉率判定结果），**非硬编码文案**。
- **实时推送**：trace 经 **WS（若接口文档已定义则沿用）或 SSE** 推送到前端；推送事件套统一信封，与既有 8.7/15.4/对话诊断的流式体例一致。
- **真实耗时**：节点时间反映实际执行耗时。
- **保留 Mock 兜底**：无密钥/演示模式仍走模拟动画可跑通，演示稳定性优先；前端"演示/实时"开关保留。
- 与 S1 已加的"工作流完成弹窗 + 跳转按钮"打通——完成弹窗里列出的应是**本次真实生成的产物**。

# 约束
- **复用 S7/B5b 的真实管线与既有 Agent 节点**，不要重建一套工作流。
- `/workflow` 及推送接口的请求/响应**字段名与层级保持不变**，只换驱动来源；新增推送事件类型须同步追加进接口文档（追加不重排）。
- 不改其他接口。

# 验证（实测并贴结果）
1. 触发一次真实生成：大屏节点按**真实 agent 执行顺序与时机**点亮（非固定节奏）；
2. 各节点消息是**真实 agent 输出**（贴出几条，证明非硬编码）；
3. 完成弹窗列出的是**本次真实生成的产物**，跳转到对应资源正确；
4. Mock 模式无密钥仍可跑通（兜底动画）；
5. 全量测试通过、既有接口回归未变。

# 输出
现状说明（任务 1）+ 改动文件清单 + 接口文档增量（如有）+ 5 项验证结果 + 红线自检。完成即停。
═══════════════════════════════════════════════════════════════

## 提示
- 任务 1 现状说明优先——若工作流早已真实驱动，CC 直接报告跳过。
- 重点盯验证第 1、2 条：**点亮节奏来自真实执行 + 消息是真实 agent 输出**。这俩是区分"真工作流"和"换皮 mock"的关键，别被"看起来动起来了但其实还是定时器"糊弄。
- 真实模式有真实延迟是正常的、也是"它真在跑"的证明；演示求稳可先预热缓存。
- 切完这条，核心链路只剩 S9（学习路径真实 Agent）。
