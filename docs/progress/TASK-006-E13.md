# TASK-006-E13 视频分镜生成边界拆分

## 完成状态

已完成 TASK-006-E 的第十三个渐进拆分边界。将神经网络五场景分镜基线、其他知识点的确定性主题 Mock 分镜、场景装配和真实模型输出清洗，从 `app/core/llm.py` 抽取到 `app/core/llm_video_script.py`。

TASK-006-E 主任务保持进行中。本轮未修改 `generate_video_script` Provider 编排、DeepSeek Prompt、视频渲染/TTS、文档检索、资源接口、前端页面或其他能力边界，未进入 TASK-006-F。

本阶段新增文件 3 个，符合每阶段新增文件不超过 8 个的约束。

## 边界与兼容性

- 神经网络五个场景的标题、要点和旁白逐字保留，迁移执行 29 行逐行一致性校验。
- `nn` 继续返回“神经网络基础”标题和五场景浅复制，不改变既有演示内容。
- 其他知识点继续按知识点名称、难度和 description 生成五段主题分镜。
- 真实输出继续过滤空标题/空旁白/空要点场景，有效场景限制 3–5 个，每场景最多 4 条要点。
- 缺少标题时继续回落当前知识点名称；有效场景不足 3 个时继续触发原有失败回落。
- `_VIDEO_SCRIPT_NN` 在 `app.core.llm` 保留兼容别名；`LLMClient._video_scene`、`_mock_video_script`、`_clean_video_script` 的签名与调用方式保持不变。
- `llm.py` 从 E12 后的 1939 行降至 1864 行。

## 文件清单

新增：

- `backend/app/core/llm_video_script.py`
- `backend/tests/test_llm_video_script.py`
- `docs/progress/TASK-006-E13.md`

修改：

- `backend/app/core/llm.py`
- `docs/维护/工作任务清单.md`
- `docs/维护/当前工程状态.md`

未修改前端、OpenAPI 快照和接口文档。工作区原有欢迎页、登录页、侧边栏、PPT、截图及其他文档改动均保留且不纳入提交。

## 启动命令

```powershell
cd backend
$env:LLM_PROVIDER='mock'
uvicorn app.main:app --port 8000
```

## 验证命令与实测结果

语法检查与专项回归：

```powershell
cd backend
python -m compileall -q app tests
python -m pytest -q tests/test_llm_video_script.py tests/test_b7a.py tests/test_document_learning.py::test_generate_diagram_and_mindmap_and_video tests/test_content_safety.py tests/test_contract_snapshot.py tests/test_document_learning.py::test_multi_doc_unified_generation
```

首次包含完整 `test_document_learning.py` 的专项运行触发已知 Chroma 磁盘段读取失败后的多文档瞬态断言；视频分镜相关用例均通过。改为精确用例并复核该多文档用例后，结果为 `102 passed, 1 warning in 24.00s`，0 failed；语法检查退出码 0。

全量回归：

```powershell
cd backend
python -m pytest -q
```

结果：`580 passed, 1 skipped, 1 warning in 163.45s`，0 failed、0 errors。唯一 warning 为既有 Starlette/httpx 弃用提示。

## 验收结论

- 本轮只拆分视频分镜基线、确定性主题 Mock 与真实输出清洗这一个能力边界。
- Provider 回落策略、Prompt、接口字段、场景内容和清洗规则保持不变。
- 精确专项与最终全量回归均为 0 error。
- TASK-006-E 后续拆分仍须逐边界独立实施、验证和提交。
