# TASK-006-E11 费曼讲解评估边界拆分

## 完成状态

已完成 TASK-006-E 的第十一个渐进拆分边界。将六类核心知识点的费曼概念覆盖库、严重度规则、确定性 Mock 评估和真实模型输出清洗，从 `app/core/llm.py` 抽取到 `app/core/llm_feynman.py`。

TASK-006-E 主任务保持进行中。本轮未修改 `feynman_eval` Provider 编排、DeepSeek Prompt、学习流程服务、康奈尔笔记、资源接口、前端页面或其他能力边界，未进入 TASK-006-F。

本阶段新增文件 3 个，符合每阶段新增文件不超过 8 个的约束。

## 边界与兼容性

- `nn/ml/dl/cnn/transformer/finetune` 六组概念库及每组四个关键概念逐字保留。
- 概念库和严重度常量迁移执行 94 行逐行一致性校验，除公开名称外完全一致。
- Mock 继续按关键词覆盖率计算 0–100 分数，并按 high、medium、low 排序缺口。
- 引导追问仍取最优先的两个缺口；存在 high/medium 缺口时 `complete=false`。
- 未收录知识点继续按 40 字阈值走长讲解通过或短讲解补充兜底，原有字段行为保持不变。
- 真实输出继续回正当前 `kpId`、非法 severity 回落 medium、分数截断 0–100、追问最多 3 条，并在缺少 feedback 时失败回落。
- `_FEYNMAN_CONCEPTS`、`_SEVERITY_RANK`、`_FEYNMAN_BLOCKING` 在 `app.core.llm` 保留兼容别名；`LLMClient._mock_feynman` 与 `_clean_feynman` 签名保持不变。
- `llm.py` 从 E10 后的 2221 行降至 2057 行。

## 文件清单

新增：

- `backend/app/core/llm_feynman.py`
- `backend/tests/test_llm_feynman.py`
- `docs/progress/TASK-006-E11.md`

修改：

- `backend/app/core/llm.py`
- `docs/维护/工作任务清单.md`
- `docs/维护/当前工程状态.md`

未修改前端、OpenAPI 快照和接口文档。工作区原有欢迎页、登录页、侧边栏、PPT、截图及其他文档改动均保留且不纳入提交。

## 启动命令

```powershell
cd backend
$env:LLM_PROVIDER='mock'
uvicorn app.main:app --port 8000
```

## 验证命令与实测结果

语法检查与专项回归：

```powershell
cd backend
python -m compileall -q app tests
python -m pytest -q tests/test_llm_feynman.py tests/test_c2_learning_flow.py tests/test_content_safety.py tests/test_contract_snapshot.py
```

结果：语法检查退出码 0；专项测试 `97 passed, 1 warning in 9.12s`，0 failed。

全量回归：

```powershell
cd backend
python -m pytest -q
```

结果：`567 passed, 1 skipped, 1 warning in 185.19s`，0 failed、0 errors。唯一 warning 为既有 Starlette/httpx 弃用提示。

## 验收结论

- 本轮只拆分费曼概念库、确定性 Mock 判定与真实输出清洗这一个能力边界。
- Provider 回落策略、Prompt、接口字段、概念文案、评分/排序/完成判定保持不变。
- 专项与全量回归均为 0 error。
- TASK-006-E 后续拆分仍须逐边界独立实施、验证和提交。
