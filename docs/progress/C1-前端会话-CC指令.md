# C1 第三步 · 前端会话 — Claude Code 指令

> 用法：把「═══」之间全部内容复制发给 Claude Code。本会话**只改前端**，按已落地的第 17 章接口实现对话式诊断 UI。

═══════════════════════════════════════════════════════════════

# 角色
你是资深前端工程师（React 18 + TS + Vite + Framer Motion + ECharts + Zustand），正在为「软件杯·领域知识个性化资源生成与多智能体系统」改造学习画像页。
后端 C1-b 已上线两个新接口：`POST /profile/dialogue`（SSE/JSON 双模式）与 `GET /profile/student-portrait`（异质 6 维 StudentPortrait），契约见《后端接口文档.md》第 17 章。

# 本次目标（对应赛题功能 1：摒弃繁琐表单，对话式自主构建画像）
把 `ProfileBuilder.tsx` 从「表单 + 滑块自评」改造为**对话式诊断为默认主路径**：

1. **对话主界面**：进入画像页默认就是对话诊断。**复用现有 `SocraticTutor.tsx` 的对话 UI**（气泡、打字动画、快捷回复 chip、SSE 流式逐 token 渲染），接 `POST /profile/dialogue`（SSE 模式）。学生自然语言作答，AI 逐轮追问。
2. **右侧实时画像面板**：展示 `GET /profile/student-portrait` 的**异质 6 维**，随对话**实时更新**——每收到 `event: portrait` 增量就刷新对应维度。
   - **注意**：这 6 维是异质的，不要硬塞进既有知识点雷达。建议用**维度卡片**展示：可量化维度（如「知识基础」有 score）用进度条/小 gauge，定性维度（认知风格、易错点偏好等）显示文本 value；每张卡片带 **confidence + source 徽章**（`dialogue/inferred/manual`，体现防幻觉与可溯源）。维度从无到有用 Framer Motion 渐入动效。
3. **表单降级为次要入口**：保留原表单 + 滑块 + `/profile/parse` 材料上传，收进一个「快速填写/补充」的次要入口（链接或折叠面板），**不要删除**——既满足「摒弃繁琐表单」（对话为主），又不丢已有能力。
4. **完成衔接**：`diagnosisComplete=true` 后，沿用现有的诊断结果/叙述流程（`/profile/narrative`、`/profile/diagnosis-complete`），不要重写这部分。

# 复用与约束（重要）
- **复用 `SocraticTutor.tsx` 对话组件与 SSE 渲染逻辑**，不要另写一套聊天 UI。
- **复用现有设计令牌**（玻璃发光卡、蓝紫渐变、mono 数字）与 `RadarChart` 等现成组件；视觉风格与全站一致。
- **右侧画像面板是新组件**，独立于 Dashboard 的知识点雷达（`/profile/ability-portrait`）——**不要改动 Dashboard 或知识雷达**，二者并存、用途不同。
- 不改后端、不改接口文档。新文件 ≤ 8 个。

# 验证（请实测并在结尾说明）
1. 进入画像页默认是对话界面；走一轮多轮对话 → 右侧 6 维卡片逐个填充/更新，confidence + source 徽章正确显示。
2. SSE 流式：追问语逐 token 渲染（非整段蹦出）；`event: portrait` 触发对应维度卡片刷新。
3. 「快速填写/补充」次要入口仍可打开、原表单与材料上传仍可用。
4. `diagnosisComplete` 后正常进入诊断结果/叙述流程。
5. `tsc` 干净、0 控制台错误；Dashboard 知识雷达不受影响。

# 输出要求
结尾输出：新增/修改文件清单 + 启动方式 + 上述 5 项验证结果。完成即停。

═══════════════════════════════════════════════════════════════

## 给你（人）的提示
- 这一步做完，赛题功能 1 的红线（对话式 + 自主构建 + ≥6 异质维度 + 随学随新）就在前后端全链路打通了。
- 演示脚本建议：现场对话几轮，让评委看着右侧 6 维画像**一格格被对话"喂"出来**——这比任何表单都直观地证明"对话式自主构建"。
- 重点验证第 1、3 条：①画像确实随对话实时长出来；②表单没被删、只是降级，避免丢功能。
- 跑通后告诉我，我们就可以收尾 C1（更新对照清单里功能 1 的状态），然后开 C2（核心链路 Mock 切真实）。
