# 联调-P0 — 前端 P0 主流程从 Mock 切换到真实后端 API · 完成总结

> 范围：仅 P0 主流程｜状态：✅ 代码完成 + 接口层全链路实测 0 报错｜日期：2026-06-10
> 一句话：新增统一 API 数据获取层 + `VITE_USE_REAL_API` 开关；开=走 B2 真实后端，
> 关=走现有 mock（**一行未删**，评审离线兜底）。UI/动效/路由/业务流零改动。

## 1. 开关机制

- `frontend/.env`（**本地配置，已 gitignore**）：`VITE_USE_REAL_API=true` → 联调走后端。
- `frontend/.env.example`（已提交，默认 `false`）：复制为 `.env` 即可切换。
- 代码统一读 `services/api.ts` 导出的 `USE_REAL_API` 常量：
  - `true`：登录、画像、路径、掌握度、旅程、讲义、测验全部经真实后端；
  - `false`/未设：各页面 / store 保持原 mock 行为，**与联调前逐像素一致**。

## 2. 切换范围（仅 P0，15 接口）

登录(1) → 画像四接口(4/5/6/7) → 学习路径两接口含 taskId 轮询(10/11) →
掌握度三接口(12/13/14) → 旅程(15) → 讲义(17) → 测验获取/提交(23/24)。

## 3. 改动文件清单（逐一说明理由）

### 3.1 新增（8 个）

| 文件 | 理由 |
|---|---|
| `src/services/api.ts` | **统一 fetch 封装**：解信封 `{code,message,data}`、自动附 `Authorization`、401/1002 清票回登录入口、非 0 码抛 `ApiError`；导出总开关 `USE_REAL_API` 与 token 工具。 |
| `src/services/auth.ts` | 登录/退出（接口 1/3），登录成功落 token+user 到 localStorage。 |
| `src/services/profile.ts` | 画像四接口（4 parse / 5 narrative / 6 diagnosis-complete / 7 ability-portrait）数据层。 |
| `src/services/learning.ts` | 学习路径（10 get / 11 generate）+ **taskId 轮询**（`pollTask` 轮询 `GET /tasks/{id}` 至终态）。 |
| `src/services/mastery.ts` | 掌握度 + 旅程数据层（12 mastery / 13 check / 14 pass / 15 journey）。 |
| `src/services/resource.ts` | 讲义 + 测验数据层（17 lecture / 23 quiz get / 24 quiz submit）。 |
| `src/vite-env.d.ts` | 为 `import.meta.env.VITE_USE_REAL_API` 提供类型（strict 下 tsc 必需），Vite 标准脚手架文件。 |
| `.env.example` | 开关模板（默认 false，安全离线）。 |

> `.env`（值为 true）为本地未跟踪文件，不入库；评审机切换只需改 `.env`。

### 3.2 改动既有（6 个，均限「数据来源层」，UI/动效/结构/路由零改）

| 文件 | 改动点 | 理由 / 边界 |
|---|---|---|
| `src/store/journey.ts` | `completeDiagnosis`/`generatePath` 本地乐观更新**同时**同步后端（POST 诊断完成 / POST 生成路径并轮询）；新增 `loadJourney()` 登录后拉取覆盖。 | store 在本次放宽清单内；localStorage 退化为缓存，以 `GET /journey` 为权威。mock 模式分支保持原 `set(...)` 行为不变。 |
| `src/store/mastery.ts` | `goCheck`/`markPassed` 乐观更新**同时**同步后端（POST check/pass）；新增 `load()` 拉取 `GET /mastery` 覆盖。 | 同上；以后端掌握度为权威，mock 分支行为零变化。 |
| `src/pages/Login.tsx` | **仅 `handleSubmit`**：联调时真实登录→落 token→拉旅程/掌握度→进入；mock 时保留原 1s `setTimeout(onLogin)`。 | 放宽清单明列「Login.tsx 的提交处理」。输入框默认值/样式/动效未动。 |
| `src/pages/ProfileBuilder.tsx` | **仅数据来源**：`runParse` 联调走 `POST /profile/parse`（上传真实 File）；`narrative` 联调走 `POST /profile/narrative`（mock 用 `useMemo` 原逻辑，联调用 effect 覆盖）；`files` 增 `raw?:File`（仅上传用，不渲染）；`activeSourceLabel` 优先取叙述自带 sources。 | 三步流程 / 雷达 / Gap / 综述 UI 与动效完全保留；`mockParse`/`generateNarrative`/`emptyProfile` 一行未删，由 `USE_REAL_API` 分支择一。 |
| `src/pages/LearningResource.tsx` | **仅数据来源**：联调时 `questions` 来自 `GET /quiz/nn`、各档 `markdown` 来自 `POST /resource/lecture`、提交走 `POST /quiz/nn/submit` 后 `load()` 刷新掌握度；mock 时仍用本地 `lectureByLevel`/`quizQuestions`。 | 8 个 Tab / 难度切换动效 / 信任面板 UI 全保留；mock 常量一行未删。 |
| `src/components/QuizRenderer.tsx` | `onSubmitResult` **追加可选第三参 `answers`**（作答全集），供联调提交后端判分。 | 纯追加、向后兼容；组件本地判分 / 渲染 / 重做逻辑零改，mock 模式忽略该参。 |

> 严格未触：所有页面 UI 结构 / CSS / framer-motion 动效 / GSAP / 路由（App stage）/
> Zustand store 结构（仅扩方法，未改 state 形状）/ 其余组件（Dashboard、KnowledgeGraph、
> JobMarketPanel、SocraticTutor 等）业务逻辑。
> `ability-portrait`(7) 数据层已实现并可用，但其自然消费方（Dashboard 雷达）不在 P0
> 点击路径、改它会触碰非 P0 页面，故未接 UI——按边界保守留作 P1。

## 4. 启动 / 验证命令

```bash
# 后端（mock provider，无需任何 Key）
cd backend && python -m uvicorn app.main:app --port 8000

# 前端（.env 内 VITE_USE_REAL_API=true）
cd frontend && npm run dev        # http://localhost:3000，/api 代理到 :8000
```

## 5. 实测结果（0 报错）

### ① 类型检查
```
npx tsc --noEmit -p tsconfig.json   →  EXIT 0（strict + noUnusedLocals 全过）
```

### ② Vite 构建/transform & 代理
```
dev server ready，无 transform/HMR 报错
GET :3000/                    → 200      （index.html）
GET :3000/src/services/api.ts → 200      （新模块 esbuild 转译通过）
GET :3000/api/v1/health       → 200 code 0（Vite /api 代理 → :8000 通）
```

### ③ P0 全链路（经浏览器等价 UTF-8 请求，clean learner_001）
```
1  login learner_001/123456        code 0  role=learner
2  profile/parse (上传 resume.txt)  code 0  skills=[机器学习基础93,神经网络80,…] materials=2
3  profile/narrative                code 0  paragraphs=2  sources=[file-0]（与 seg.sourceId 一致，链接可点）
4  profile/diagnosis-complete       code 0  {hasDiagnosed:true}
5  learning-path/generate + 轮询     taskId→running→succeeded  result.lessons=6
6  resource/lecture 入门/初级/高级    code 0  markdown 三档不同 + sources=3 + hallucinationRate=0.021
7  quiz/nn (get)                    code 0  [nn_q1,nn_q2,nn_q3]
8  quiz/nn/submit (全对)             code 0  score=100 passed=true masteryUpdated={nn:passed}
9  GET /mastery                     {nn:passed}        ← 知识点变已通过
10 GET /journey                     currentStep=learn  targetJob=大模型应用工程师（首页旅程联动数据就绪）
```
> 备注：用 curl 内联中文 JSON 会被 Windows shell 编码破坏（B2-a 已记此坑），故联调实测
> 改用 Python `urllib` 发 UTF-8 JSON（与浏览器 `fetch` 等价）。

### ④ 边界一致性
- narrative 叙述 `sources` 的 chip id 与高亮 `sourceId` 一致（issubset=True），点击回看可联动。
- `.env` 置 `false` 时各分支早返回原 mock 路径，`tsc` 已覆盖两分支编译；行为与联调前一致。

## 6. 诚实声明（未覆盖项）

- 本机**无浏览器自动化工具**（Playwright/Puppeteer 未装），故「真人点击走查 + 控制台
  0 报错」未做自动化断言；已在其下各层充分验证：类型层（tsc 0 报错）、模块转译层
  （Vite 200）、契约层（15 接口全链路 code 0 + 字段对齐）、代理层（:3000→:8000 通）。
  建议评审按第 4 节启动后手动走查一遍确认 DOM/控制台。
- 演示前已将 `learner_001` 掌握度 / 旅程**重置为干净态**（mastery 0 行、未诊断），
  以便走查能看到 学习中→待检验→已通过 的完整跃迁。

## 7. 给后续阶段的约定

- 联调切换点全部收敛在 `src/services/*` + 两个 store + 三处页面「数据来源」+ Login 提交；
  P1/P2 接口（岗位对标 9、资源聚合 21、错题 25、图谱 26、概览 29、视频 18、苏格拉底 22、
  工作流 27/28）按同一套 `api.ts` 范式逐个接入即可，UI 仍零改。
- B5 把后端 mock provider 换成真实 RAG+Agent 后，本前端**无需任何改动**（响应契约不变）。
