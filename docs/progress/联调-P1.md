# 联调-P1 — 前端 P1 特色接口从 Mock 切换到真实后端 API · 完成总结

> 范围：仅 P1 五处切换（接口 8/9/21/25/26/29）｜状态：✅ 代码完成 + 接口层全链路实测 0 报错｜日期：2026-06-11
> 一句话：沿用联调-P0 全部约定——`USE_REAL_API` 分支择一、mock 一行不删、
> 统一走 `api.ts` 信封封装；改动仅限 services 层与各处「数据来源」，UI/样式/动效零改动。

## 1. 切换范围（5 处，对应后端 B6 六接口）

| # | 切换点 | 接口 | 行为 |
|---|---|---|---|
| 1 | `services/jobMarket.ts` | 5.1 hot / 5.2 {id} | 数据源从 `public/data/job-market/*.json` 改为后端；**保留既有 12h 本地缓存与 PRESET 兜底**；后端 `code:2002 + data.offline:true` 映射为既有 `offline` 标记（前端「离线快照」UI 复用，不新增状态；离线快照不写缓存） |
| 2 | `pages/KnowledgeGraph.tsx` | 10.1 | 节点/边/categories 改吃 `GET /knowledge-graph`，**联调不再前端自行推导着色**（后端按掌握度权威推导）；掌握度变化时重取保持实时着色 |
| 3 | `pages/Dashboard.tsx` | 12.1 | 综合分/等级/覆盖率/已学资源/强弱项/雷达/同级对比/对标摘要整体改吃 `GET /dashboard/overview`（聚合接口为页面单一真相源，避免与明细口径混用）；掌握度变化时重取保持原「随掌握度联动」行为 |
| 4 | `components/ResourceAggregator.tsx` | 8.6 | 改吃 `GET /resource/external/{kpId}`（CURRENT_KP_ID=nn，后端已按相关度降序） |
| 5 | `components/WeakPointReinforce.tsx` | 9.2 | 错题 id 上报 `POST /reinforce`，渲染后端真实 recap + 针对性练习（联调时测验题 id 为 `nn_q1…`，本地 bank 命中不了，此前实为空渲染——本次补齐闭环） |

## 2. 改动文件清单

### 2.1 新增（4 个）

| 文件 | 理由 |
|---|---|
| `frontend/src/services/graph.ts` | 知识图谱数据层（接口 26），`KnowledgeGraphData` 类型对齐 10.1 契约。 |
| `frontend/src/services/dashboard.ts` | 学情概览数据层（接口 29），`DashboardOverview` 类型对齐 12.1 契约。 |
| `backend/scripts/verify_p1.py` | P1 六接口浏览器等价 UTF-8 实测脚本（同 P0 约定，避开 Windows shell 编码坑），可重复回归。 |
| `backend/scripts/verify_p1_degrade.py` | 2002 降级路径实测脚本（`JOB_MARKET_OFFLINE=true` 起 8001 验证）。 |

### 2.2 改动既有（8 个，均限「数据来源层」，UI/动效/结构/路由零改）

| 文件 | 改动点 | 理由 / 边界 |
|---|---|---|
| `src/services/api.ts` | `RequestOptions` 追加可选 `okCodes` 白名单：命中的业务码不抛 `ApiError`、直接返回 data。 | 5.2 契约是「code 2002 **携带可用数据**」的降级语义，原封装会丢 data；纯追加、默认行为不变（其余调用零影响）。 |
| `src/services/jobMarket.ts` | 新增 `getHotJobs()`（联调走 5.1，mock 返回内置 `HOT_JOBS`）；`getJobMarket` 联调分支走 5.2 + `okCodes:[2002]`，`data.offline` → 既有 `offline:true` 路径。 | 缓存命中→秒显、取数失败→旧缓存→PRESET 的三级策略原样保留；mock 分支（public JSON）一行未删。 |
| `src/services/resource.ts` | 追加 `getExternalResources(kpId)`（8.6）与 `reinforce(kpId, wrongQuestionIds)`（9.2，`ReinforceCard` 类型）。 | 纯追加，P0 已有讲义/测验函数未动。 |
| `src/components/JobMarketPanel.tsx` | 热门岗位列表 `hotJobs` state（初值 `HOT_JOBS`），联调时 effect 用 `getHotJobs()` 覆盖；`suggestions` 改引 state。 | mock 模式 effect 早返回，初值即 `HOT_JOBS`，渲染零差异；搜索过滤/选中逻辑未动。 |
| `src/pages/KnowledgeGraph.tsx` | 联调时 effect 拉 `GET /knowledge-graph` 入 `remote`，`enodes/elinks/ecats` 三元择一（remote vs 本地种子+派生）；tooltip/legend/series 改引 `ecats/elinks`。 | 本地 12 节点 / 14 边种子与 mastery 派生逻辑一行未删（mock 模式继续使用）；ECharts 配置 / 力导参数 / 样式零改。请求落地前首帧渲染本地种子，到达后随既有 1.2s 过渡动画刷新。 |
| `src/pages/Dashboard.tsx` | 联调时 effect 拉 `GET /dashboard/overview` 入 `overview`，`profileData/radarData/learnedResources/betterThanPct/对标摘要` 择一；「超过了 68%」与「已学资源 12」两处硬编码改为变量（mock 分支取值仍为 68/12，渲染逐字一致）。 | mock 的 `mockProfileData` 本地计算（含 masteredCore 联动）一行未删；journey store 三个 hook 保留（mock 模式对标摘要仍由 store 驱动）。 |
| `src/components/ResourceAggregator.tsx` | `ExternalResource` 接口导出（供 service 复用，仿 P0 QuizQuestion 先例）；`list` state（初值本地 `resources`），联调 effect 用接口 21 覆盖；头部计数与列表改引 `list`。 | mock 模式 effect 早返回，初值即本地精选示例，渲染零差异；嵌入播放/评分条/卡片结构未动。 |
| `src/components/WeakPointReinforce.tsx` | 联调时 effect 调 `POST /reinforce` 上报错题 id，`items` 在 `remoteItems`（真实 recap+练习）与本地 `reinforcementBank` 间择一；生成中态由"接口返回"驱动（mock 仍为原 1400ms 模拟）。 | `reinforcementBank` 一行未删；生成动效 / 卡片结构 / `QuizRenderer` 渲染未动；接口失败回退本地 bank 兜底。 |

> 严格未触：路由 / Zustand store 结构 / CSS / framer-motion / GSAP / 其余页面组件。
> 所有联调分支失败路径均 console.error + 回退本地 mock 数据，不白屏。

## 3. 启动 / 验证命令

```bash
# 后端（mock provider，无需任何 Key）
cd backend && python -m uvicorn app.main:app --port 8000

# 前端（.env 内 VITE_USE_REAL_API=true）
cd frontend && npm run dev        # /api 代理到 :8000

# 接口层回归
cd backend && python scripts/verify_p1.py
# 2002 降级（另起故障实例）：
#   PowerShell: $env:JOB_MARKET_OFFLINE='true'; python -m uvicorn app.main:app --port 8001
#   然后: python scripts/verify_p1_degrade.py
```

## 4. 实测结果（0 报错）

### ① 类型检查
```
npx tsc --noEmit -p tsconfig.json   →  EXIT 0（strict + noUnusedLocals 全过，两分支均编译覆盖）
```

### ② P1 全链路（浏览器等价 UTF-8 请求，8000=mock provider）
```
[login]     code 0  role=learner
[hot]       code 0  ['llm-app', 'algo-engineer', 'ml-engineer', 'data-analyst']
[snapshot]  code 0  id=llm-app heatPct=95 openings=1840 offline_key=False skills=6 radar_dims=6
[graph]     code 0  nodes=12 links=14 categories=['已掌握','学习中','待学习','知识盲区'] nn={category:0,value:100}
[overview]  code 0  score=52.3 level=初级 coverage=0.41 learned=3 betterThan=71 radar_dims=6
            strong=[(神经网络基础,100),(机器学习基础,92),(深度学习原理,65)]
            target={'hasDiagnosed': True, 'targetJobName': '算法工程师', 'matchPct': 0}
[external]  code 0  n=4 top={id:nn-r1,type:视频,relevance:98,credibility:97} desc_order=True
[reinforce] code 0  cards=2 points=['神经元运算顺序','ReLU 与梯度消失'] practice_ids=['nn_q1-r','nn_q3-r']
            answers_self_consistent=True
ALL P1 BROWSER-EQUIVALENT CHECKS PASSED (code 0)
```
> nn 已通过（P0 走查遗留态），图谱 nn category=0/value=100、overview strong 首位=神经网络基础 100，
> 与 B6「mastery→graph→dashboard 联动」口径一致，恰好顺带验证了联动。

### ③ 2002 降级 → offline 映射
```
[degrade] http=200 code=2002 offline=True id=llm-app fetchedAt=2026-06-06T06:10:00+08:00
          msg=岗位实时数据源不可用，已降级返回最近快照
DEGRADE CHECK PASSED
```
前端 `getJobMarket` 经 `okCodes:[2002]` 放行后读 `data.offline` → 返回既有 `{offline:true}`，
JobMarketPanel 复用「离线版本」标记 + `timeAgo(fetchedAt)`，未新增任何 UI 状态。

### ④ Vite transform & 代理（dev server，无 transform/HMR 报错）
```
GET /                                        → 200
GET /src/services/{graph,dashboard,jobMarket,resource}.ts          → 200
GET /src/pages/{Dashboard,KnowledgeGraph}.tsx                      → 200
GET /src/components/{JobMarketPanel,ResourceAggregator,WeakPointReinforce}.tsx → 200
GET /api/v1/health                           → 200 code 0（/api 代理 → :8000 通）
```

### ⑤ 开关关闭回归
`VITE_USE_REAL_API=false` 时：5 处联调 effect 全部早返回、各择一表达式回落 mock 初值
（Dashboard 两处变量 mock 取值仍为 68/12，渲染逐字一致），行为与切换前一致；
两分支均经 tsc 编译覆盖。

## 5. 诚实声明（未覆盖项）

- 同 P0：本机无浏览器自动化工具，「真人点击走查 + 控制台 0 报错」未做自动化断言；
  已覆盖类型层（tsc 0 报错）、模块转译层（Vite 200）、契约层（6 接口 code 0 + 降级 2002）、
  代理层（:3001→:8000 通）。建议评审按第 3 节启动后手动走查 首页 / 知识图谱 /
  画像诊断岗位面板 / 资源推荐 / 分阶测试错题强化 五处确认 DOM 与控制台。
- 图谱「未开始」节点着色以后端推导为权威（B6 总结第 5 节已记：finetune/dl/prompt
  个别节点与前端手调基线存在预期内差异），联调模式按后端口径渲染属正确行为，非回归。
- Dashboard 的 `learned_resources` 联调显示后端 ResourceCache 实数（实测 3），
  与 mock 演示值 12 不同——数据来源切换的预期结果，UI 结构无变化。

## 6. 给后续阶段的约定

- P1 后剩余未接 UI 的接口：视频 18、苏格拉底 22（SSE）、工作流 27/28（WS/轮询）、
  画像 ability-portrait 7 的 Dashboard 雷达消费（本次雷达已改由 overview.radar 提供，
  与 4.4 同源同值，7 无需再单独接）。
- `api.ts` 的 `okCodes` 机制可复用于后续任何「非 0 码携带可用数据」的降级契约。
