# CC 指令 · 即时辅导（改名回「即时辅导」+ 补实时反馈三件套）

> 承接：常驻入口（AskTutorDock 侧边 dock + 抽屉）已做好。本会话①把命名统一回「即时辅导」；②在现有 dock 基础上**增量**补齐实时反馈三件套。**不要重做入口**。
> 全局纪律：已联调接口签名禁改、复用现有、Mock/真实双模式、完成即停、tsc 干净 0 报错。

═══════════════════════════════════════════════════════════════

# 角色
资深前端工程师。项目已联调，禁改既有接口签名。本会话在现有 AskTutorDock / TutorResourcePanel 基础上增量改造，复用 /resource/tutor/suggest + /generate + 苏格拉底 SSE 流式接口，不改后端签名。

# 任务 0：命名统一为「即时辅导」
把当前「AI 答疑」相关文案全局统一改为「**即时辅导**」（dock 标签、抽屉标题、按钮文案等）。这是已确认的最终命名，全局替换即可。

# 任务 1（B-1）：流式实时回答
- 即时辅导的问题识别 / 答疑回答改为 **SSE 流式逐字呈现**（复用现有苏格拉底 tutor_chat_stream 流式能力），不要转圈等待后整坨返回。
- 抽屉里回答逐字蹦出；流式中可见"正在思考/生成"态；结束后再出资源清单。

# 任务 2（B-2）：选中讲义提问（升级现有"针对当前讲义答疑"）
- 现状：已有一个「📖 针对当前讲义答疑」按钮，但只是整篇泛指。
- 升级为**选中即问**：用户在讲义阅读时**选中一段文字**，就近弹出「就这段问 AI」快捷操作，点击把**选中的具体文字**作为问题上下文发起即时辅导，让针对性具体到句子。
- 保留原整篇按钮作为兜底；选中为空时不弹。

# 任务 3（B-3）：主动察觉卡顿
- 当学习行为出现"卡住信号"时，AI **主动**弹出轻量关怀提示（如"这块是不是有点难？要我帮你讲讲吗？"），点击即进入即时辅导（带当前知识点上下文）。
- 卡住信号用现有可得行为数据判定：如同一知识点测验出错、某步骤停留时长异常、重复访问同一内容。
- **限频是关键**：同一知识点 / 同一会话内最多提示 1 次，已被关闭则不再打扰；提示要轻量（小卡片/气泡），不打断当前操作。

# 约束
- 增量改造，**不重做已完成的常驻入口**；复用现有 dock/抽屉/TutorResourcePanel/SSE/行为数据。
- 不改后端签名（如需新增仅追加并同步文档）；深浅主题正常；mock 兜底。

# 验证（截图）
1. 全局命名为「即时辅导」，无残留「AI 答疑」；
2. 流式：辅导回答逐字实时蹦出（非整坨等待）；
3. 选中讲义某句 → 出现「就这段问 AI」→ 能就该句发起辅导并生成资源；
4. 构造卡住信号（连续答错/反复看同一点）→ AI 主动弹轻量关怀提示；**同一知识点/会话限频、关闭后不再弹**；
5. 既有链路（识别问题→清单→生成、视频走 Remotion 自然语音）仍正常；不与"去通关"抢焦点；tsc 干净、0 console error。

# 输出
现状说明 + 改动文件清单 + 接口文档增量（如有）+ 验证结果 + 红线自检。完成即停。

═══════════════════════════════════════════════════════════════

## 提示（给你人看）
- 重点盯任务 3 的**限频**——演示弹一次惊艳、弹五次烦人。务必"同一知识点/会话最多 1 次、关了不再弹"。
- 任务 1 流式、任务 2 选中提问是最抓评委的，验收亲自走一遍体感顺不顺。
- 别让 CC 重做入口——它只该在现有 dock 上加东西。

═══════════════════════════════════════════════════════════════

# 执行完成总结（2026-06-24）

## 一、现状说明（改造前 → 改造后）
- **改造前**：常驻入口 `AskTutorDock`（侧边 dock + 右滑抽屉）已做好，提问后**转圈等待**整坨返回资源清单；文案为「AI 答疑」；「针对当前讲义答疑」只是整篇泛指；无任何主动察觉。
- **改造后**：在**不重做入口**的前提下增量补齐——①全局命名统一为「即时辅导」；②提问改为 **SSE 逐字流式回答**（复用 8.7 苏格拉底 `tutor_chat` 流式，mock 本地逐字兜底），答完再出资源清单；③讲义阅读**选中即问**（就近浮「就这段问 AI」，把具体句子作上下文）；④行为出现**卡住信号**时主动弹轻量关怀小卡（严格限频）。所有链路复用既有接口，**后端零改动**。

## 二、改动文件清单
**新增（6）**
| 文件 | 作用 |
| --- | --- |
| `frontend/src/services/tutorBus.ts` | 即时辅导「打开总线」：选中提问 / 主动关怀无需透传 props 即可命令 dock 打开并自动发起一次提问（轻量发布订阅） |
| `frontend/src/services/stuckSignal.ts` | 卡住信号累计 + 广播（阈值 `STUCK_THRESHOLD=2`），限频/去重由订阅方负责 |
| `frontend/src/components/SelectionAskBubble.tsx` | B-2 选中即问：监听讲义容器内选区，就近浮出「就这段问 AI」 |
| `frontend/src/components/StuckNudge.tsx` | B-3 主动关怀小卡：达阈值弹一次，sessionStorage 按知识点限频 |
| `frontend/src/components/InstantTutorExtras.css` | 选中气泡 + 关怀小卡样式（语义令牌，双主题联动） |
| — | （新文件计 5 个 .ts/.tsx + 1 个 .css，未超 8 个/阶段约束） |

**修改（5）**
| 文件 | 改动 |
| --- | --- |
| `frontend/src/services/tutor.ts` | 新增 `streamInstantReply()`：联调走真实 SSE、mock/失败逐字兜底；token 由 `control.cancelled` 可中止 |
| `frontend/src/components/AskTutorDock.tsx` | 命名→即时辅导；提问先**逐字流式回答**（思考态 + 光标）再出资源清单；订阅 `tutorBus` 支持外部预填+自动发起；关闭/卸载中止流 |
| `frontend/src/components/AskTutorDock.css` | 命名注释；新增回答气泡 / 闪烁光标 / 思考三点动画样式 |
| `frontend/src/components/SocraticTutor.tsx` | 按钮文案「AI 答疑」→「即时辅导」 |
| `frontend/src/pages/LearningResource.tsx` | 讲义正文挂 `SelectionAskBubble`；测验不及格/错题多、同一讲解内容反复打开→`reportStuck`；页面挂 `StuckNudge` |

## 三、接口文档增量
**无。** 未新增/修改任何后端接口或字段，全部复用既有：`POST /resource/tutor/chat`（SSE 流式）、`POST /resource/tutor/suggest`、`POST /resource/tutor/generate`、`8.6` 外部聚合。后端签名零改动。

## 四、验证结果（联调真实后端 learner_001，端口 3002，实测截图逐项通过）
1. **命名统一**：dock 标签 `即时辅导`、抽屉标题 `即时辅导 · 针对性答疑`、Socratic 按钮 `即时辅导 · 生成针对性资源`；全局 `grep "AI ?答疑"` **0 残留**。✅
2. **流式实时回答**：提问「反向传播梯度怎么算」→ 🦉 气泡逐字吐出真实 SSE 答复（链式法则引导），**答完才**渲染资源清单（识别问题点 + 4 项资源 + 资源推荐）。后端 `tutor.sse_stream` 按 token `yield delta`，前端逐 delta 追加，确属真逐字非整坨。✅
3. **选中讲义提问**：定制讲义正文选中一句 → 就近浮「💡 就这段问 AI」→ 点击打开即时辅导，问题预填为该句、自动发起，答复明确围绕选中句（"单个神经元表达能力有限"）并生成资源。✅
4. **主动察觉卡顿 + 限频**：反复打开同一讲义（第 3 次）触发信号 → 右下角弹 🦉「这块是不是有点难？」关怀小卡（不遮挡 dock、不抢「去通关」焦点）；点「先不用」关闭后，**再触发更多信号不再复弹**（`sessionStorage instant-tutor-nudge:nn` 守卫，同知识点/会话仅 1 次）。✅
5. **既有链路无回归**：资源中枢 6 卡 / 逐项生成 / 难度自适应讲义 / 视频 Remotion 入口均在；`tsc --noEmit` **0 报错**；功能页交互期间 Playwright 控制台 **0 error**（聚合里的 `:3005` 连接拒绝为残留 HMR socket、`401 /auth/login` 为手动输错密码所致，均非本功能引入）。深/浅（鼠尾草/墨纸）双主题均正常。✅

## 五、红线自检
- [x] 未改任何后端接口路径/字段/枚举；未新增后端依赖。
- [x] 未重做常驻入口（dock/抽屉/TutorResourcePanel 均增量复用）。
- [x] 未改 Zustand store 结构 / 路由；仅在辅导功能组件与 `services/` 数据获取层增量。
- [x] Mock 兜底完整：`streamInstantReply` 无后端/失败时本地逐字；选中提问与关怀小卡均不依赖真实 Key。
- [x] 限频是硬约束：同一知识点/会话最多 1 次、关闭后不再打扰（已实测）。
- [x] `tsc` 干净、0 console error；双主题正常；不与「去通关」抢焦点。
