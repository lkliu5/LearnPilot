# CC 指令 · 会话二补充（主资源生成选择性化 + 新用户 500 修复）

> 接会话二：选择性生成 + 资源推荐进成果，智能辅导面板（TutorResourcePanel）已做；本会话把同一套搬到**主资源生成**，并补做被漏掉的新用户接口 500 修复。
> 全局纪律：已联调接口签名禁改、统一信封、Mock/真实双模式、复用优先、完成即停、tsc 干净 0 报错。

═══════════════════════════════════════════════════════════════

# 角色
资深前端/全栈工程师。项目已联调，禁改既有接口签名。本会话两件事。

## 任务 1：主资源生成也改为选择性生成 + 逐项进度 + 资源推荐进成果
**现状（先查并报告）**：定位**主资源生成**组件（学习资源页里生成 讲义/视频/图解/思维导图/代码 的地方，**不是** TutorResourcePanel）。确认它是不是一次性批量生成、要等全部完成才整体显示。

**改**：把会话二在 `TutorResourcePanel` 里已建好的那套**直接复用搬过来**——
- 用户**勾选/选择要生成的资源类型** → **逐项调用既有单类型生成接口** → 每项独立状态（待生成/生成中/已完成/失败）+ 顶部总进度条 → **生成完一项就呈现一项**，不必等全部完成；
- 把**资源推荐**（联网聚合外部资源）也并入主资源生成的**成果区**，与讲义/视频/图解并列、可打开（复用会话二新增的 `fetchRecommendations`）；
- **复用会话二的状态/进度 UI 组件与样式**，保持两处体验一致，别另造一套。

**约束**：复用既有单类型生成接口（types 传单元素逐次调用，契约不变）+ `fetchRecommendations`；不改接口签名；mock 兜底。

**验证**：主资源生成处勾选若干类型 → 逐项生成、逐项出结果、进度可见、未勾选的不生成；资源推荐并入主成果区可打开；与智能辅导面板体验一致。

## 任务 2（补做·会话二漏掉的任务4）：新用户接口 500 → 200 空态
**现象**：新用户/空数据时，`student-portrait` 与 `knowledge-graph` 接口返回 500（被前端 .catch 兜住，但 console 报红、录演示难看）。
**改**：让这两个接口对"新用户/空数据"返回**干净空态**——200 + 统一信封 + 零值/空结构，而非 500。
**验证**：用全新用户调这两个接口返回 200 空态，前端 console 无报错。

# 输出
现状说明（任务1 定位的主生成组件）+ 改动文件清单 + 2 项验证结果 + 红线自检。完成即停。

═══════════════════════════════════════════════════════════════

## 提示（给你人看）
- 任务1 关键是**复用**会话二在智能辅导面板里已做好的逐项进度/状态那套，搬到主资源生成，别让两处长得不一样。
- 验收任务1 盯"逐项呈现、进度可见、未选不生成、资源推荐在主成果区"；任务2 盯"新用户调接口不再 500、console 无报红"。
- 跑完连同会话二一起单独 commit。
