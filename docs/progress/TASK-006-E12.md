# TASK-006-E12 错题强化生成边界拆分

## 完成状态

已完成 TASK-006-E 的第十二个渐进拆分边界。将三道神经网络错题强化预置卡、未收录题目的确定性 Mock 变式，以及真实模型强化结果的契约解析与自洽审核，从 `app/core/llm.py` 抽取到 `app/core/llm_reinforcement.py`。

TASK-006-E 主任务保持进行中。本轮未修改 `generate_reinforcement` Provider 编排、DeepSeek Prompt、两次审核重试策略、题库服务、文档测验、前端页面或其他能力边界，未进入 TASK-006-F。

本阶段新增文件 3 个，符合每阶段新增文件不超过 8 个的约束。

## 边界与兼容性

- `nn_q1`～`nn_q3` 三张预置强化卡逐字保留，迁移执行 55 行逐行一致性校验。
- 预置卡继续复制 practice 后返回，调用方修改结果不会污染全局题库。
- 通用 Mock 变式继续轮转选项一位，同时保留 option_id 与 correct_answer 的自洽关系。
- 真实输出继续按输入错题数截断，数量不足、缺 practice、题目 id 漂移和题目自洽问题均按原规则报告。
- `audit_practice` 仍作为正确答案、选项和解析自洽性的统一审核入口。
- DeepSeek 仍保留“首次生成 + 携带审核意见重试一次”的原有策略。
- `_REINFORCE_BANK` 在 `app.core.llm` 保留兼容别名；`LLMClient._mock_reinforce_card` 与 `_clean_reinforcement` 签名保持不变。
- `llm.py` 从 E11 后的 2057 行降至 1939 行。

## 文件清单

新增：

- `backend/app/core/llm_reinforcement.py`
- `backend/tests/test_llm_reinforcement.py`
- `docs/progress/TASK-006-E12.md`

修改：

- `backend/app/core/llm.py`
- `docs/维护/工作任务清单.md`
- `docs/维护/当前工程状态.md`

未修改前端、OpenAPI 快照和接口文档。工作区原有欢迎页、登录页、侧边栏、PPT、截图及其他文档改动均保留且不纳入提交。

## 启动命令

```powershell
cd backend
$env:LLM_PROVIDER='mock'
uvicorn app.main:app --port 8000
```

## 验证命令与实测结果

语法检查与专项回归：

```powershell
cd backend
python -m compileall -q app tests
python -m pytest -q tests/test_llm_reinforcement.py tests/test_b6.py tests/test_content_safety.py tests/test_contract_snapshot.py
```

结果：语法检查退出码 0；专项测试 `99 passed, 1 warning in 13.38s`，0 failed。

全量回归：

```powershell
cd backend
python -m pytest -q
```

首次运行出现 1 个与本阶段无关的 `test_multi_doc_unified_generation` 瞬态失败，日志显示 Chroma 磁盘段读取失败后降级内存检索；该用例单独复跑为 `1 passed`。随后重新执行完整套件，最终结果为 `574 passed, 1 skipped, 1 warning in 143.30s`，0 failed、0 errors。唯一 warning 为既有 Starlette/httpx 弃用提示。

## 验收结论

- 本轮只拆分错题强化预置库、确定性 Mock 变式与真实输出审核清洗这一个能力边界。
- Provider 重试、Prompt、接口字段、卡片文案、变式规则和自洽审核保持不变。
- 专项与最终全量回归均为 0 error。
- TASK-006-E 后续拆分仍须逐边界独立实施、验证和提交。
